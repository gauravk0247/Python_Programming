# Q. Can you change the values inside a list which is contained in set S
s = {8, 7, 12, "Gaurav", [1,2]}
print(s)

# This Qusetion is wrong
# beacause in the dictionary list cannot allowed and
# tuple can't be changed